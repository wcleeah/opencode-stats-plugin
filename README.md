# opencode-stats-plugin

Combined usage tracker plugin and Turso maintenance tools for OpenCode.

The plugin exposes maintenance actions as OpenCode tools and writes detailed
run logs to `~/.local/share/opencode/usage-tracker/logs/`.
